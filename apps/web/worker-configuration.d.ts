// Cloudflare Pages / Wrangler type-check placeholder.
// Keep this committed so `wrangler types --check` has a stable target in CI and Pages builds.
declare namespace Cloudflare {
  interface Env {}
}
