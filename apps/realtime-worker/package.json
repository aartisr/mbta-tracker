{
  "name": "mbta-realtime-worker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node ./scripts/require-node-22.mjs && wrangler dev",
    "deploy": "node ./scripts/require-node-22.mjs && wrangler deploy",
    "deploy:dry-run": "node ./scripts/require-node-22.mjs && wrangler deploy --dry-run",
    "test": "vitest run"
  },
  "dependencies": {
    "@mbta/transit-core": "*"
  },
  "devDependencies": {
    "vitest": "^2.1.9",
    "wrangler": "^4.26.0"
  }
}
